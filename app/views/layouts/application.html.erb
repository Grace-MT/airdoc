<!DOCTYPE html>
<html>
  <head>
    <title>AirDoc</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>




    <%= render 'pages/header' %>

    <% if notice && notice == 'no such patient exist' %>
      <p class="notifications fail"><%= notice %></p>
    <% elsif notice%>
      <p class="notifications success"><%= notice %></p>
    <% end %>

    <% if alert %>
      <p class="notifications success"><%= alert %></p>
    <% end %>


    <%= yield %>
  </body>
</html>
